apiVersion: v1
kind: Service
metadata:
  name: detection-service
spec:
  type: NodePort
  selector:
    app: detection
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30007